import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';

const ListProduct = ({ socket, product }) => {

    const navigate = useNavigate();

    return (
        <div>
            
        </div>
    );

};

export default ListProduct;