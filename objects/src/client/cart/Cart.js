import React, { useState } from 'react';
import { useNavigate } from 'react-router-dom';

const Cart = ({ socket }) => {

    const navigate = useNavigate();

    return (
        <div>
            
        </div>
    );

};

export default Cart;